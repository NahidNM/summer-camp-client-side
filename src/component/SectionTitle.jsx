

const SectionTitle = ({title}) => {
    return (
        <div>
        <div className="divider"></div>
            <div className="text-3xl font-semibold text-center divider">{title}</div>
            <div className="divider"></div>
            </div>
    );
};

export default SectionTitle;